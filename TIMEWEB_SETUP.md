# Настройка сервера на Timeweb

## 📋 Параметры сервера

### Рекомендуемая конфигурация:
- **ОС**: Ubuntu 24.04 LTS ✅
- **Регион**: Москва ✅
- **CPU**: 1 x 3.3 ГГц ✅
- **RAM**: 2 ГБ ✅
- **Диск**: 30 ГБ NVMe ✅
- **Публичный IP**: Да ✅
- **Бэкапы**: Включены ✅

**Итого**: ~880 ₽/мес

## 🚀 Шаг 1: Создание сервера

### В интерфейсе Timeweb:

1. **Образ**: Ubuntu 24.04 ✅
2. **Регион**: Москва ✅
3. **Конфигурация**: 
   - 1 CPU, 2 GB RAM, 30 GB ✅
4. **Сеть**:
   - IPv4: Публичный IP ✅
   - IPv6: Публичный IP (опционально)
5. **Дополнительные услуги**:
   - Бэкапы: 1 раз в день ✅
   - Защита от DDoS: Рекомендуется включить
6. **Авторизация**:
   - **ВАЖНО**: Загрузите SSH ключ (см. ниже)
   - Или разрешите доступ по паролю (менее безопасно)
7. **Cloud-init**: Можно пропустить (настроим вручную)
8. **Имя сервера**: `kreo-production` или `kreo-prod`
9. **Проект**: KREO ✅

### Создание SSH ключа (если еще нет):

```bash
# На вашем локальном компьютере
ssh-keygen -t rsa -b 4096 -C "kreo-timeweb" -f ~/.ssh/kreo-timeweb

# Просмотреть публичный ключ для загрузки в Timeweb
cat ~/.ssh/kreo-timeweb.pub
```

**Скопируйте весь вывод** и загрузите в Timeweb в разделе "Авторизация" → "Загрузить новый ключ"

## 📝 Шаг 2: Получение данных сервера

После создания сервера в панели Timeweb вы получите:

1. **IP адрес сервера** - сохраните его!
   - Пример: `185.123.45.67`
   - Находится в карточке сервера

2. **SSH доступ**:
   - Пользователь: `root` (обычно для Ubuntu)
   - Порт: `22` (стандартный)
   - Пароль: Если не использовали SSH ключ

3. **Данные для подключения**:
   ```bash
   ssh root@YOUR_SERVER_IP
   ```

## 🔧 Шаг 3: Первоначальная настройка сервера

### Подключение к серверу:

```bash
# Если использовали SSH ключ
ssh -i ~/.ssh/kreo-timeweb root@YOUR_SERVER_IP

# Если используете пароль
ssh root@YOUR_SERVER_IP
```

### Автоматическая настройка:

```bash
# Скачать и запустить скрипт настройки
curl -fsSL https://raw.githubusercontent.com/your-username/kreo-it/main/scripts/setup-server.sh | bash

# Или скопировать скрипт вручную
```

### Ручная настройка (если нужно):

```bash
# 1. Обновление системы
apt-get update && apt-get upgrade -y

# 2. Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
rm get-docker.sh

# 3. Установка Docker Compose
apt-get install -y docker-compose-plugin

# 4. Установка Nginx
apt-get install -y nginx

# 5. Установка Certbot для SSL
apt-get install -y certbot python3-certbot-nginx

# 6. Создание директорий
mkdir -p /opt/kreo-it
mkdir -p /var/www/kreo-it/production
mkdir -p /var/log/nginx

# 7. Настройка прав
chown -R $USER:$USER /opt/kreo-it
chown -R www-data:www-data /var/www/kreo-it
```

## 🌐 Шаг 4: Настройка DNS

### В панели управления доменом (у регистратора):

Добавьте A-запись:

```
Тип: A
Имя: @ (или оставить пустым для корневого домена)
Значение: [IP вашего сервера из Timeweb]
TTL: 3600
```

Для www (опционально):
```
Тип: CNAME
Имя: www
Значение: kreo.pro
TTL: 3600
```

### Проверка DNS:

```bash
# Проверить, что DNS записи настроены
nslookup kreo.pro

# Должен вернуть IP вашего сервера
```

**Важно**: DNS изменения могут распространяться до 24-48 часов.

## 🔒 Шаг 5: Получение SSL сертификата

После того, как DNS записи распространились:

```bash
# На сервере
certbot --nginx -d kreo.pro -d www.kreo.pro

# Certbot автоматически:
# - Получит SSL сертификат
# - Настроит Nginx для HTTPS
# - Настроит автообновление
```

## 📦 Шаг 6: Настройка проекта на сервере

### Копирование файлов:

```bash
# На вашем локальном компьютере
# Сначала соберите проект
cd /Users/antontusnakov/Desktop/kreo-it
npm run build:production

# Создайте Docker образ
./scripts/deploy.sh

# Скопируйте файлы на сервер
scp -i ~/.ssh/kreo-timeweb \
  kreo-it-production.tar.gz \
  docker-compose.yml \
  docker/nginx-production.conf \
  root@YOUR_SERVER_IP:/opt/kreo-it/
```

### На сервере:

```bash
# Подключитесь к серверу
ssh -i ~/.ssh/kreo-timeweb root@YOUR_SERVER_IP

# Перейдите в директорию проекта
cd /opt/kreo-it

# Загрузите Docker образ
docker load < kreo-it-production.tar.gz

# Скопируйте Nginx конфигурацию
cp docker/nginx-production.conf /etc/nginx/sites-available/kreo.pro
ln -s /etc/nginx/sites-available/kreo.pro /etc/nginx/sites-enabled/

# Проверьте конфигурацию Nginx
nginx -t

# Перезапустите Nginx
systemctl reload nginx

# Запустите Docker контейнер
docker-compose up -d production
```

## 🔐 Шаг 7: Настройка GitHub Secrets

В настройках GitHub репозитория:
**Settings → Secrets and variables → Actions → New repository secret**

Добавьте:

| Secret Name | Значение |
|------------|----------|
| `PRODUCTION_SERVER_HOST` | IP адрес сервера из Timeweb |
| `PRODUCTION_SERVER_USER` | `root` |
| `PRODUCTION_SERVER_SSH_KEY` | Содержимое `~/.ssh/kreo-timeweb` (приватный ключ) |
| `PRODUCTION_SERVER_PORT` | `22` |

**Как получить приватный ключ:**
```bash
cat ~/.ssh/kreo-timeweb
# Скопируйте ВЕСЬ вывод (включая -----BEGIN и -----END)
```

## ✅ Шаг 8: Проверка работоспособности

### На сервере:

```bash
# Проверить Docker контейнер
docker ps
docker logs kreo-it-production

# Проверить Nginx
systemctl status nginx
nginx -t

# Проверить доступность
curl http://localhost/health
```

### В браузере:

1. Откройте https://kreo.pro
2. Проверьте SSL сертификат (должен быть зеленый замочек)
3. Проверьте, что сайт загружается
4. Проверьте консоль браузера на ошибки

## 🔄 Шаг 9: Первый автоматический деплой

После настройки GitHub Secrets:

```bash
# На вашем локальном компьютере
git add .
git commit -m "Настройка деплоя на Timeweb"
git push origin main
```

GitHub Actions автоматически:
1. Соберет проект
2. Создаст Docker образ
3. Задеплоит на сервер
4. Проверит работоспособность

## 🛠️ Дополнительная настройка

### Firewall (UFW):

```bash
# На сервере
ufw allow 22/tcp    # SSH
ufw allow 80/tcp    # HTTP
ufw allow 443/tcp   # HTTPS
ufw enable
```

### Мониторинг:

```bash
# Логи Docker
docker-compose logs -f production

# Логи Nginx
tail -f /var/log/nginx/kreo.pro.access.log
tail -f /var/log/nginx/kreo.pro.error.log

# Ресурсы сервера
docker stats
df -h
free -h
```

## 📊 Чеклист готовности

- [ ] Сервер создан в Timeweb
- [ ] SSH ключ загружен в Timeweb
- [ ] Подключение к серверу работает
- [ ] Скрипт setup-server.sh выполнен
- [ ] DNS записи настроены
- [ ] DNS записи распространились (проверка через nslookup)
- [ ] SSL сертификат получен
- [ ] Nginx конфигурация скопирована
- [ ] Docker контейнер запущен
- [ ] GitHub Secrets настроены
- [ ] Сайт доступен по https://kreo.pro
- [ ] Автоматический деплой работает

## 🆘 Решение проблем

### Не могу подключиться по SSH:
- Проверьте, что SSH ключ загружен в Timeweb
- Проверьте IP адрес сервера
- Проверьте firewall на сервере

### DNS не резолвится:
- Подождите 24-48 часов
- Проверьте настройки DNS у регистратора
- Используйте `dig kreo.pro` для проверки

### SSL сертификат не получается:
- Убедитесь, что DNS записи распространились
- Проверьте, что домен указывает на правильный IP
- Проверьте, что порты 80 и 443 открыты

### Docker контейнер не запускается:
- Проверьте логи: `docker logs kreo-it-production`
- Проверьте, что порт 80 не занят другим процессом
- Проверьте конфигурацию docker-compose.yml

## 📞 Полезные команды Timeweb

В панели Timeweb вы можете:
- Перезагрузить сервер
- Посмотреть статистику использования ресурсов
- Управлять бэкапами
- Мониторить сетевой трафик
- Управлять firewall

---

**Готово!** После выполнения всех шагов ваш сайт будет доступен на https://kreo.pro с автоматическим деплоем через GitHub Actions.

