# Требуемая информация для подключения сервера

## 📋 Чеклист необходимых данных

### 1. Данные VPS сервера

#### Обязательные данные:
- [ ] **IP адрес сервера** или доменное имя сервера
  - Пример: `185.123.45.67` или `server.kreo.pro`
  - Где найти: В панели управления VPS провайдера

- [ ] **SSH пользователь**
  - Обычно: `root` или созданный пользователь (например, `deploy`)
  - Где найти: Создается при настройке сервера

- [ ] **SSH приватный ключ**
  - Файл с расширением `.pem` или без расширения
  - Где найти: Создается при настройке SSH доступа
  - Важно: Это приватный ключ, не публичный!

- [ ] **SSH порт** (опционально, по умолчанию 22)
  - Обычно: `22`
  - Где найти: В настройках SSH на сервере

#### Как получить SSH ключ (если его нет):

**Вариант 1: Создать новый ключ**
```bash
# На вашем локальном компьютере
ssh-keygen -t rsa -b 4096 -C "your_email@example.com" -f ~/.ssh/kreo-it-deploy

# Это создаст два файла:
# ~/.ssh/kreo-it-deploy (приватный ключ - нужен для GitHub)
# ~/.ssh/kreo-it-deploy.pub (публичный ключ - нужен на сервере)
```

**Вариант 2: Использовать существующий ключ**
```bash
# Если у вас уже есть SSH ключ
ls ~/.ssh/
# Обычно: id_rsa, id_ed25519 и т.д.
```

**Добавить публичный ключ на сервер:**
```bash
# Скопировать публичный ключ на сервер
ssh-copy-id -i ~/.ssh/kreo-it-deploy.pub user@your-server-ip

# Или вручную:
cat ~/.ssh/kreo-it-deploy.pub
# Скопировать вывод и добавить в ~/.ssh/authorized_keys на сервере
```

### 2. Данные домена

#### Для kreo.pro:
- [ ] **Домен настроен** и указывает на IP сервера
  - DNS A запись: `kreo.pro` → IP сервера
  - DNS CNAME запись: `www.kreo.pro` → `kreo.pro` (опционально)
  - Где настроить: В панели управления доменом

**Проверка DNS:**
```bash
# Проверить, что домен указывает на правильный IP
nslookup kreo.pro
```

### 3. Данные для GitHub Secrets

Все эти данные нужно добавить в настройках GitHub репозитория:
**Settings → Secrets and variables → Actions → New repository secret**

#### Для продакшен окружения (kreo.pro):

| Secret Name | Значение | Пример |
|------------|----------|--------|
| `PRODUCTION_SERVER_HOST` | IP или домен сервера | `185.123.45.67` или `server.kreo.pro` |
| `PRODUCTION_SERVER_USER` | SSH пользователь | `root` или `deploy` |
| `PRODUCTION_SERVER_SSH_KEY` | Приватный SSH ключ (весь файл) | Содержимое `~/.ssh/kreo-it-deploy` |
| `PRODUCTION_SERVER_PORT` | SSH порт (опционально) | `22` |

**Как получить содержимое SSH ключа:**
```bash
# На вашем локальном компьютере
cat ~/.ssh/kreo-it-deploy

# Скопируйте ВЕСЬ вывод (включая -----BEGIN и -----END строки)
# И вставьте в GitHub Secret PRODUCTION_SERVER_SSH_KEY
```

### 4. Опциональные данные

#### Docker Hub (если хотите использовать):
- [ ] **DOCKER_USERNAME** - логин Docker Hub
- [ ] **DOCKER_PASSWORD** - пароль Docker Hub
- Где найти: На docker.com

*Примечание: Можно обойтись без Docker Hub, образы будут передаваться напрямую через SSH*

## 📝 Пошаговая инструкция

### Шаг 1: Подготовка SSH ключа

```bash
# 1. Создать SSH ключ (если нет)
ssh-keygen -t rsa -b 4096 -f ~/.ssh/kreo-it-deploy

# 2. Добавить публичный ключ на сервер
ssh-copy-id -i ~/.ssh/kreo-it-deploy.pub root@your-server-ip

# 3. Проверить подключение
ssh -i ~/.ssh/kreo-it-deploy root@your-server-ip

# 4. Получить приватный ключ для GitHub
cat ~/.ssh/kreo-it-deploy
```

### Шаг 2: Настройка DNS

В панели управления доменом (у регистратора) добавить:

```
Тип: A
Имя: @ (или оставить пустым)
Значение: [IP вашего сервера]
TTL: 3600

Тип: CNAME (опционально)
Имя: www
Значение: kreo.pro
TTL: 3600
```

### Шаг 3: Добавление Secrets в GitHub

1. Откройте репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Добавьте каждый секрет:

**PRODUCTION_SERVER_HOST:**
- Name: `PRODUCTION_SERVER_HOST`
- Secret: `185.123.45.67` (ваш IP)

**PRODUCTION_SERVER_USER:**
- Name: `PRODUCTION_SERVER_USER`
- Secret: `root`

**PRODUCTION_SERVER_SSH_KEY:**
- Name: `PRODUCTION_SERVER_SSH_KEY`
- Secret: (весь содержимое приватного ключа)

**PRODUCTION_SERVER_PORT:**
- Name: `PRODUCTION_SERVER_PORT`
- Secret: `22`

### Шаг 4: Первоначальная настройка сервера

```bash
# Подключиться к серверу
ssh root@your-server-ip

# Скачать и запустить скрипт настройки
curl -fsSL https://raw.githubusercontent.com/your-username/kreo-it/main/scripts/setup-server.sh | bash

# Или вручную скопировать скрипт и запустить
```

## ✅ Чеклист готовности

Перед первым деплоем убедитесь:

- [ ] VPS сервер запущен и доступен
- [ ] SSH ключ создан и добавлен на сервер
- [ ] DNS записи настроены для домена kreo.pro
- [ ] DNS записи распространились (проверка через `nslookup`)
- [ ] Все GitHub Secrets добавлены
- [ ] Скрипт `setup-server.sh` выполнен на сервере
- [ ] SSL сертификаты получены (через Certbot)
- [ ] Nginx конфигурация скопирована на сервер

## 🔍 Проверка подключения

### Проверка SSH:
```bash
ssh -i ~/.ssh/kreo-it-deploy root@your-server-ip
# Должно подключиться без запроса пароля
```

### Проверка DNS:
```bash
nslookup kreo.pro
# Должен возвращать IP вашего сервера
```

### Проверка GitHub Secrets:
1. Откройте Actions в GitHub
2. Попробуйте запустить workflow вручную
3. Проверьте логи на наличие ошибок подключения

## 🆘 Частые проблемы

### Проблема: "Permission denied (publickey)"
**Решение:**
- Убедитесь, что публичный ключ добавлен в `~/.ssh/authorized_keys` на сервере
- Проверьте права доступа: `chmod 600 ~/.ssh/authorized_keys`

### Проблема: "Connection refused"
**Решение:**
- Проверьте, что SSH порт открыт в firewall
- Проверьте, что SSH сервис запущен: `systemctl status ssh`

### Проблема: DNS не резолвится
**Решение:**
- Подождите 24-48 часов для распространения DNS
- Проверьте настройки DNS у регистратора
- Используйте `dig` или `nslookup` для проверки

## 📞 Что нужно от вас

**Минимум для начала:**
1. IP адрес VPS сервера
2. SSH пользователь (обычно `root`)
3. Возможность добавить SSH ключ на сервер
4. Доступ к настройкам DNS домена kreo.pro

**После настройки:**
- Все данные будут в GitHub Secrets
- Деплой будет автоматическим при пуше в `main`
- Никаких дополнительных действий не потребуется
