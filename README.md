# Kreo IT - Digital AI Agency Platform

Платформа для digital-агентства Kreo с публичным лендингом.

## 📋 Содержание

- [Архитектура проекта](#архитектура-проекта)
- [Структура проекта](#структура-проекта)
- [Технологический стек](#технологический-стек)
- [Быстрый старт](#быстрый-старт)
- [Разработка](#разработка)
- [Деплой](#деплой)
- [Безопасность](#безопасность)

## 🏗️ Архитектура проекта

### Домены

- **kreo.pro** - Публичный лендинг (Next.js)

### Инфраструктура

- **VPS сервер**: Timeweb (46.149.67.149)
- **Веб-сервер**: Nginx (reverse proxy)
- **Процесс-менеджер**: PM2
- **SSL**: Let's Encrypt (автообновление)
- **Файрвол**: UFW
- **Защита**: Fail2ban

## 📁 Структура проекта

```
kreo-it/
├── src/                        # Next.js лендинг
│   ├── app/                    # App Router
│   │   ├── layout.jsx         # Корневой layout
│   │   ├── page.jsx           # Главная страница
│   │   ├── kejsy/             # Страница кейсов
│   │   └── globals.css        # Глобальные стили
│   ├── components/            # React компоненты
│   │   ├── layout/            # Header, Footer, Breadcrumbs
│   │   ├── ui/                # UI компоненты (shadcn/ui)
│   │   ├── CustomCursor.jsx   # Кастомный курсор
│   │   ├── SplineViewer.jsx   # 3D Spline компонент
│   │   └── LeadForm.jsx       # Форма обратной связи
│   ├── constants/             # Константы (cases, contacts)
│   ├── lib/                   # Утилиты
│   └── styles/                # Дополнительные стили
├── scripts/                   # Скрипты
│   ├── deploy.sh              # Деплой на сервер
│   ├── git-push.sh           # Коммит и пуш в Git
│   ├── setup-clean-server.sh # Настройка чистого сервера
│   └── security-hardening.sh # Настройка защиты
├── nginx/                     # Конфигурации Nginx
│   └── kreo.pro.conf         # Конфигурация для домена
├── next.config.js            # Конфигурация Next.js
├── tailwind.config.js        # Конфигурация Tailwind CSS
└── package.json              # Зависимости и скрипты
```

## 🛠️ Технологический стек

### Лендинг (kreo.pro)
- **Framework**: Next.js 15.5.7 (App Router)
- **UI**: React 18.3.1
- **Стилизация**: Tailwind CSS
- **Анимации**: Framer Motion
- **UI компоненты**: shadcn/ui (Radix UI)
- **3D графика**: Spline (@splinetool/react-spline)
- **Шрифты**: Inter, JetBrains Mono (Google Fonts)
- **Иконки**: Lucide React

## 🚀 Быстрый старт

### Требования

- Node.js 20+
- npm или yarn

### Установка

```bash
# Клонирование репозитория
git clone https://github.com/kreokreo/kreo-sait.git
cd kreo-sait

# Установка зависимостей
npm install
```

### Запуск локально

```bash
# Разработка
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

## 💻 Разработка

### Команды

```bash
# Разработка
npm run dev              # Запуск Next.js dev сервера
npm run build            # Сборка для production
npm run start            # Запуск production сервера

# Линтинг
npm run lint             # Проверка кода
npm run lint:fix         # Автоисправление
```

### Структура компонентов

```
src/
├── app/              # Next.js App Router
│   ├── layout.jsx   # Корневой layout с метаданными
│   └── page.jsx     # Страницы
├── components/      # React компоненты
│   ├── layout/     # Header, Footer, Breadcrumbs
│   ├── ui/         # Переиспользуемые UI компоненты
│   └── ...         # Специальные компоненты
├── constants/       # Константы (CONTACTS, cases)
├── lib/            # Утилиты (cn, createPageUrl)
└── styles/         # Дополнительные стили
```

### Path Aliases

Проект использует path aliases для удобного импорта:

```javascript
import { CONTACTS } from '@/constants'
import { cn } from '@/lib/utils'
import Header from '@/components/layout/Header'
```

### Стилизация

- **Tailwind CSS** - основная система стилей
- **CSS переменные** - для темизации и брендинга
- **Кастомные стили** - в `src/app/globals.css` и `src/styles/typography.css`

### 3D графика (Spline)

Проект использует Spline для интерактивной 3D графики:

```jsx
import SplineViewer from '@/components/SplineViewer'

<SplineViewer url="https://prod.spline.design/..." />
```

Компонент автоматически:
- Загружается только на клиенте (SSR отключен)
- Оптимизирует обработку событий мыши
- Обрабатывает ошибки загрузки

## 🐳 Работа с Git и Деплой

### Работа с Git

Скрипт для коммита и пуша изменений в Git:

```bash
# С описанием изменений
npm run git:push "описание изменений"

# Или интерактивно (скрипт запросит описание)
npm run git:push

# Или напрямую
./scripts/git-push.sh "описание изменений" [ветка]
```

**Что делает:**
1. ✅ Показывает изменения
2. ✅ Запрашивает подтверждение
3. ✅ Добавляет все изменения (`git add -A`)
4. ✅ Создает коммит с описанием
5. ✅ Отправляет в указанную ветку (по умолчанию текущая)

### Деплой на сервер

Скрипт для деплоя проекта на сервер:

```bash
# Деплой текущей ветки
npm run deploy
```

**Что делает:**
1. ✅ Подключается к серверу по SSH
2. ✅ Клонирует/обновляет репозиторий из Git на сервере
3. ✅ Устанавливает зависимости (`npm ci`)
4. ✅ Собирает проект на сервере (`npm run build`)
5. ✅ Копирует статические файлы в standalone директорию
6. ✅ Запускает через PM2 (автоматический перезапуск при сбоях)
7. ✅ Проверяет доступность приложения
8. ✅ Настраивает автозапуск PM2 при перезагрузке сервера

**Настройка перед первым деплоем:**

Создайте файл `.env.deploy` в корне проекта:

```bash
SERVER=root@46.149.67.149:22
GIT_REPO_URL=https://github.com/kreokreo/kreo-sait.git
GIT_BRANCH=main
DEPLOY_PATH=/opt/kreo-it
```

**Требования на сервере:**
- Node.js 20+
- PM2 (установится автоматически через `npm run setup:server`)
- Nginx (для проксирования запросов)
- Git

**Типичный workflow:**

```bash
# 1. Внесли изменения в код
# 2. Коммитим и пушим в Git
npm run git:push "feat: добавлена новая функция"

# 3. Деплоим на сервер
npm run deploy
```

### Настройка сервера

```bash
# Первоначальная настройка VPS (после переустановки ОС)
npm run setup:server

# Настройка защиты от уязвимостей
./scripts/security-hardening.sh
```

Подробные инструкции по настройке сервера см. в `SETUP_SERVER.md`.

### Nginx конфигурация

Конфигурация Nginx находится в `nginx/kreo.pro.conf` и включает:
- SSL сертификаты (Let's Encrypt)
- Проксирование к Next.js приложению
- Gzip сжатие
- Security headers
- Кеширование статических файлов

## 🔒 Безопасность

### Защита от уязвимостей

Проект защищен от известных уязвимостей:

- ✅ **Next.js 15.5.7** - исправляет CVE-2025-66478
- ✅ **React 18.3.1** - не уязвим (CVE-2025-55182 затрагивает только React 19.x)

### Настроенные меры защиты

1. **Файрвол (UFW)** - блокирует исходящие подключения к вредоносным IP
2. **Fail2ban** - защита от брутфорса SSH
3. **Мониторинг майнеров** - ежедневная автоматическая проверка
4. **SSL сертификаты** - автоматическое обновление через Certbot

Подробнее см. `SECURITY_PLAN.md` и `SECURITY_AUDIT.md`.

## 🎨 Дизайн система

### Цвета

- **Brand**: Синий (`hsl(210, 100%, 50%)`)
- **Background**: Белый
- **Foreground**: Темно-серый
- **Grid**: Светло-серый фон с сеткой

### Типографика

- **Заголовки**: Inter, жирные, с отрицательным letter-spacing
- **Текст**: Inter, средний размер
- **Моноширинный**: JetBrains Mono

### Компоненты

Все UI компоненты основаны на shadcn/ui и адаптированы под бренд.

## 📝 Структура карты сайта

Карта сайта находится в файле `sitemap(json)` и содержит:
- Все страницы сайта
- SEO ключевые слова
- Цены для страниц услуг
- Метаданные

## 📚 Дополнительная информация

- **Документация Next.js**: https://nextjs.org/docs
- **Tailwind CSS**: https://tailwindcss.com/docs
- **Spline**: https://spline.design
- **shadcn/ui**: https://ui.shadcn.com
- **Framer Motion**: https://www.framer.com/motion

## 📄 Лицензия

Private - все права защищены © 2025 Kreo
