# Kreo IT - IT & AI Agency Platform

Платформа для IT-компании Kreo с публичным лендингом и внутренним приложением для командной работы.

## 📋 Содержание

- [Архитектура проекта](#архитектура-проекта)
- [Структура проекта](#структура-проекта)
- [Технологический стек](#технологический-стек)
- [Быстрый старт](#быстрый-старт)
- [Разработка](#разработка)
- [Деплой](#деплой)
- [API документация](#api-документация)

## 🏗️ Архитектура проекта

### Домены

- **kreo.pro** - Публичный лендинг (Next.js)
- **app.kreo.pro** - Внутреннее приложение для команды (React + Node.js API)

### Инфраструктура

- **VPS сервер**: Timeweb (31.130.155.38)
- **Веб-сервер**: Nginx (reverse proxy)
- **Контейнеризация**: Docker + Docker Compose
- **SSL**: Let's Encrypt (автообновление)
- **CI/CD**: GitHub Actions

## 📁 Структура проекта

```
kreo-it/
├── src/                        # Next.js лендинг (kreo.pro)
│   ├── app/                    # App Router
│   │   ├── layout.jsx         # Корневой layout
│   │   ├── page.jsx           # Главная страница
│   │   └── globals.css        # Глобальные стили
│   ├── components/            # React компоненты
│   │   ├── layout/            # Header, Footer, Breadcrumbs
│   │   ├── ui/                # UI компоненты (shadcn/ui)
│   │   ├── CustomCursor.jsx   # Кастомный курсор
│   │   └── SplineScene.jsx    # 3D Spline компонент
│   ├── constants/             # Константы
│   ├── lib/                   # Утилиты
│   └── styles/                # Дополнительные стили
├── apps/                      # Внутреннее приложение (app.kreo.pro)
│   └── app/
│       ├── backend/           # Node.js + Express API
│       │   ├── src/
│       │   │   ├── config/    # Конфигурация (database)
│       │   │   ├── models/    # Модели данных (User)
│       │   │   ├── controllers/ # Контроллеры (authController)
│       │   │   ├── routes/    # Роуты (authRoutes)
│       │   │   ├── middleware/ # Middleware (auth)
│       │   │   └── utils/     # Утилиты (jwt)
│       │   └── database/     # SQL скрипты
│       └── frontend/          # React + Vite приложение
│           ├── src/
│           │   ├── api/       # API клиент
│           │   ├── pages/     # Страницы (Login, Register)
│           │   ├── components/ # Компоненты (ProtectedRoute)
│           │   ├── store/     # Zustand store
│           │   └── lib/       # Утилиты
├── docker/
│   ├── Dockerfile             # Для лендинга (Next.js)
│   ├── Dockerfile.backend     # Для backend API
│   ├── Dockerfile.frontend   # Для frontend приложения
│   └── nginx-*.conf           # Конфигурации Nginx
├── .github/workflows/
│   └── deploy-production.yml  # CI/CD для деплоя
├── scripts/                   # Скрипты деплоя
│   ├── deploy.sh              # Ручной деплой
│   ├── setup-server.sh        # Настройка сервера
│   └── setup-app-domain.sh    # Настройка поддомена
├── docker-compose.yml         # Оркестрация контейнеров
├── next.config.js            # Конфигурация Next.js
├── tailwind.config.js        # Конфигурация Tailwind CSS
├── postcss.config.js         # Конфигурация PostCSS
├── jsconfig.json             # Path aliases для Next.js
└── .env                      # Переменные окружения (не в git)
```

## 🛠️ Технологический стек

### Лендинг (kreo.pro)
- **Framework**: Next.js 15 (App Router)
- **UI**: React 18
- **Стилизация**: Tailwind CSS
- **Анимации**: Framer Motion
- **UI компоненты**: shadcn/ui
- **3D графика**: Spline (@splinetool/react-spline)
- **Шрифты**: Inter, JetBrains Mono (Google Fonts)

### Внутреннее приложение (app.kreo.pro)

#### Backend
- **Runtime**: Node.js 18+
- **Framework**: Express 5
- **База данных**: MySQL 8.0
- **Аутентификация**: JWT (access + refresh tokens)
- **Валидация**: Zod
- **Хеширование**: bcrypt

#### Frontend
- **UI**: React 18 + Vite
- **Стилизация**: Tailwind CSS
- **Роутинг**: React Router
- **State Management**: Zustand
- **HTTP клиент**: Axios
- **Forms**: React Hook Form

#### Инфраструктура
- **Контейнеризация**: Docker + Docker Compose
- **Веб-сервер**: Nginx
- **Кеш**: Redis (опционально)
- **CI/CD**: GitHub Actions

## 🚀 Быстрый старт

### Требования

- Node.js 18+
- npm или yarn
- Docker и Docker Compose (для деплоя)
- MySQL 8.0 (для локальной разработки backend)

### Установка

```bash
# Клонирование репозитория
git clone https://github.com/kreokreo/kreo-it.git
cd kreo-it

# Установка зависимостей для лендинга
npm install

# Установка зависимостей для внутреннего приложения
cd apps/app/backend
npm install

cd ../frontend
npm install
```

### Запуск локально

#### Лендинг (Next.js)

```bash
# Из корня проекта
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

#### Backend API

```bash
cd apps/app/backend

# Создайте .env файл с настройками БД
cp .env.example .env

# Запустите сервер
npm start
```

API будет доступно по адресу: http://localhost:3000

#### Frontend приложения

```bash
cd apps/app/frontend

# Создайте .env файл
cp .env.example .env

# Запустите dev сервер
npm run dev
```

Приложение будет доступно по адресу: http://localhost:5174

## 💻 Разработка

### Лендинг (Next.js)

```bash
# Разработка
npm run dev

# Сборка для production
npm run build

# Запуск production сборки
npm run start

# Линтинг
npm run lint
npm run lint:fix
```

### Структура компонентов

```
src/
├── app/              # Next.js App Router
│   ├── layout.jsx   # Корневой layout с метаданными
│   └── page.jsx     # Страницы
├── components/      # React компоненты
│   ├── layout/      # Header, Footer, Breadcrumbs
│   ├── ui/          # Переиспользуемые UI компоненты
│   └── ...          # Специальные компоненты
├── constants/       # Константы (CONTACTS и т.д.)
├── lib/             # Утилиты (cn, createPageUrl)
└── styles/          # Дополнительные стили
```

### Path Aliases

Проект использует path aliases для удобного импорта:

```javascript
import { CONTACTS } from '@/constants'
import { cn } from '@/lib/utils'
import Header from '@/components/layout/Header'
```

### Стилизация

- **Tailwind CSS** - основная система стилей
- **CSS переменные** - для темизации и брендинга
- **Кастомные стили** - в `src/app/globals.css` и `src/styles/typography.css`

### 3D графика (Spline)

Проект использует Spline для интерактивной 3D графики:

```jsx
import SplineScene from '@/components/SplineScene'

<SplineScene sceneUrl="https://prod.spline.design/..." />
```

Компонент автоматически:
- Загружается только на клиенте (SSR отключен)
- Оптимизирует обработку событий мыши
- Обрабатывает ошибки загрузки

## 🐳 Деплой

### Варианты деплоя

1. **Прямой деплой (без Docker)** - `npm run deploy:direct` ⚡
2. **Деплой через Docker** - `npm run deploy` 🐳
3. **GitHub Actions** - автоматически при push в main

### Прямой деплой без Docker (рекомендуется)

Самый простой способ - деплой без Docker, напрямую на сервер:

```bash
npm run deploy:direct
```

**Что делает:**
1. ✅ Собирает Next.js проект локально
2. ✅ Создает архив с файлами (.next, public, package.json)
3. ✅ Копирует архив на сервер
4. ✅ Распаковывает и устанавливает зависимости на сервере
5. ✅ Запускает через PM2 (автоматический перезапуск)
6. ✅ Обновляет Nginx конфигурацию

**Требования на сервере:**
- Node.js 18+
- PM2 (установится автоматически)
- Nginx

### Деплой через Docker

Локальный деплой на production (требует Docker):

Деплой выполняется напрямую с локальной машины на сервер одной командой:

```bash
# Основной скрипт деплоя
npm run deploy

# Или напрямую
./scripts/deploy-one-command.sh
```

**Что делает скрипт:**
1. ✅ Собирает Next.js проект (`npm run build`)
2. ✅ Создает Docker образ
3. ✅ Сохраняет образ в архив
4. ✅ Копирует файлы на сервер (образ, docker-compose.yml, nginx конфигурация)
5. ✅ Загружает образ на сервере
6. ✅ Останавливает старый контейнер
7. ✅ Запускает новый контейнер
8. ✅ Обновляет Nginx конфигурацию с полным перезапуском
9. ✅ Проверяет доступность приложения

**Настройка перед первым деплоем:**

Создайте файл `.env.deploy` в корне проекта:

```bash
PRODUCTION_SERVER_HOST=your-server-ip
PRODUCTION_SERVER_USER=your-username
PRODUCTION_SERVER_SSH_KEY=~/.ssh/id_rsa
PRODUCTION_SERVER_PORT=22
DEPLOY_PATH=/opt/kreo-it
```

Или экспортируйте переменные окружения:

```bash
export PRODUCTION_SERVER_HOST=your-server-ip
export PRODUCTION_SERVER_USER=your-username
export PRODUCTION_SERVER_SSH_KEY=~/.ssh/id_rsa
```

**Проверка после деплоя:**

```bash
# Проверка статуса контейнера
ssh -i ~/.ssh/id_rsa user@server 'docker ps | grep kreo-it-production'

# Просмотр логов
ssh -i ~/.ssh/id_rsa user@server 'docker logs kreo-it-production --tail 50'

# Или используйте скрипт диагностики
./scripts/check-server.sh
```

### GitHub Actions (опционально)

Автоматический деплой через GitHub Actions также настроен, но не является основным способом:

1. Push в ветку `main` или `master`
2. GitHub Actions автоматически выполнит деплой

**Необходимые Secrets в GitHub:**
- `PRODUCTION_SERVER_HOST`
- `PRODUCTION_SERVER_USER`
- `PRODUCTION_SERVER_SSH_KEY`
- `PRODUCTION_SERVER_PORT` (опционально)

### Настройка сервера

```bash
# Первоначальная настройка VPS
./scripts/setup-server.sh

# Настройка поддомена app.kreo.pro
./scripts/setup-app-domain.sh
```

### Nginx конфигурация

Конфигурации Nginx находятся в `docker/`:
- `nginx-production.conf` - для лендинга (kreo.pro)
- `nginx-app.conf` - для внутреннего приложения (app.kreo.pro)

## 📡 API документация

### Backend API (app.kreo.pro/api)

#### Аутентификация

**POST** `/api/auth/register`
- Регистрация нового пользователя

**POST** `/api/auth/login`
- Вход в систему
- Возвращает access и refresh tokens

**POST** `/api/auth/refresh`
- Обновление access token

**POST** `/api/auth/logout`
- Выход из системы

**GET** `/api/auth/me`
- Получение информации о текущем пользователе
- Требует авторизации

### Переменные окружения

#### Лендинг (.env)

```env
NODE_ENV=production
NEXT_PUBLIC_APP_URL=https://kreo.pro
```

#### Backend (apps/app/backend/.env)

```env
NODE_ENV=production
PORT=3000
DB_HOST=localhost
DB_PORT=3306
DB_NAME=kreo_app
DB_USER=kreo_user
DB_PASSWORD=your_password
JWT_SECRET=your_jwt_secret
JWT_REFRESH_SECRET=your_refresh_secret
```

## 🔧 Полезные команды

```bash
# Разработка
npm run dev              # Запуск Next.js dev сервера
npm run build            # Сборка для production
npm run start            # Запуск production сервера

# Docker
npm run docker:build     # Сборка Docker образа
docker-compose up -d     # Запуск контейнеров
docker-compose logs -f   # Просмотр логов

# Git
git status               # Статус изменений
git add .                # Добавить все изменения
git commit -m "..."      # Коммит
git push                 # Отправить в репозиторий
```

## 📝 Структура карты сайта

Карта сайта находится в файле `sitemap(json)` и содержит:
- Все страницы сайта
- SEO ключевые слова
- Цены для страниц услуг
- Метаданные

## 🎨 Дизайн система

### Цвета

- **Brand**: Кроваво-красный (`hsl(0, 80%, 45%)`)
- **Background**: Белый
- **Foreground**: Темно-серый

### Типографика

- **Заголовки**: Inter, жирные, с отрицательным letter-spacing
- **Текст**: Inter, средний размер
- **Моноширинный**: JetBrains Mono

### Компоненты

Все UI компоненты основаны на shadcn/ui и адаптированы под бренд.

## 📚 Дополнительная информация

- **Документация Next.js**: https://nextjs.org/docs
- **Tailwind CSS**: https://tailwindcss.com/docs
- **Spline**: https://spline.design
- **shadcn/ui**: https://ui.shadcn.com

## 📄 Лицензия

Private - все права защищены © 2025 Kreo
