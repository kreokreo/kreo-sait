# 🚀 Инструкция по деплою одной командой

## Быстрый старт

### 1. Настройка (только один раз)

Создайте файл `.env.deploy` в корне проекта:

```bash
cp .env.deploy.example .env.deploy
```

Заполните данные вашего сервера:

```bash
PRODUCTION_SERVER_HOST=31.130.155.38
PRODUCTION_SERVER_USER=root
PRODUCTION_SERVER_SSH_KEY=~/.ssh/id_rsa
PRODUCTION_SERVER_PORT=22
DEPLOY_PATH=/opt/kreo-it
```

### 2. Деплой одной командой

```bash
npm run deploy
```

Всё! Скрипт автоматически:
- ✅ Соберет проект
- ✅ Создаст Docker образ
- ✅ Скопирует файлы на сервер
- ✅ Загрузит образ на сервер
- ✅ Перезапустит контейнер
- ✅ Очистит временные файлы

## Альтернатива: через переменные окружения

Если не хотите создавать `.env.deploy`, можно экспортировать переменные:

```bash
export PRODUCTION_SERVER_HOST=31.130.155.38
export PRODUCTION_SERVER_USER=root
export PRODUCTION_SERVER_SSH_KEY=~/.ssh/id_rsa
export PRODUCTION_SERVER_PORT=22

npm run deploy
```

## Проверка деплоя

После деплоя проверьте:

```bash
# Подключитесь к серверу
ssh -i ~/.ssh/id_rsa root@31.130.155.38

# Проверьте статус контейнера
cd /opt/kreo-it
docker compose ps

# Проверьте логи
docker compose logs landing
```

## Решение проблем

### Ошибка: "Не заданы переменные окружения"
- Убедитесь, что файл `.env.deploy` существует и заполнен
- Или экспортируйте переменные в терминале

### Ошибка: "Permission denied" при SSH
- Проверьте права доступа к SSH ключу: `chmod 600 ~/.ssh/id_rsa`
- Убедитесь, что ключ добавлен на сервер: `ssh-copy-id -i ~/.ssh/id_rsa root@31.130.155.38`

### Ошибка: "Cannot connect to Docker daemon"
- Убедитесь, что Docker установлен и запущен на сервере
- Проверьте права пользователя: `sudo usermod -aG docker $USER`

## GitHub Actions (автоматический деплой)

Если настроены GitHub Secrets, деплой происходит автоматически при push в `main`:

1. Перейдите в Settings → Secrets and variables → Actions
2. Добавьте секреты:
   - `PRODUCTION_SERVER_HOST`
   - `PRODUCTION_SERVER_USER`
   - `PRODUCTION_SERVER_SSH_KEY`
   - `PRODUCTION_SERVER_PORT` (опционально)

3. Push в main:
   ```bash
   git push origin main
   ```

4. Проверьте статус: https://github.com/kreokreo/kreo-it/actions

