# Руководство по разработке

## 🏗️ Архитектура разработки

Проект использует упрощенную архитектуру с двумя окружениями:

1. **Локальная разработка** - на вашем компьютере
2. **Продакшен** - на сервере (kreo.pro)

## 💻 Локальная разработка

### Запуск проекта

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev
```

Проект будет доступен по адресу: `http://localhost:5173`

### Сборка для проверки

```bash
# Сборка production версии локально
npm run build:production

# Просмотр собранной версии
npm run preview
```

### Docker локально (опционально)

```bash
# Сборка Docker образа
npm run docker:build

# Запуск контейнера
npm run docker:up

# Просмотр логов
npm run docker:logs

# Остановка
npm run docker:down
```

## 🚀 Деплой в продакшен

### Автоматический деплой (рекомендуется)

1. **Подготовка:**
   - Настроить GitHub Secrets (см. `SERVER_REQUIREMENTS.md`)
   - Настроить сервер (см. `SERVER_SETUP.md`)

2. **Деплой:**
   - Сделать commit и push в `main` branch
   - GitHub Actions автоматически:
     - Соберет проект
     - Создаст Docker образ
     - Задеплоит на сервер
     - Проверит работоспособность

3. **Проверка:**
   - Откройте https://kreo.pro
   - Проверьте, что все работает

### Ручной деплой

```bash
# 1. Сборка и создание образа
./scripts/deploy.sh

# 2. Копирование на сервер
scp kreo-it-production.tar.gz docker-compose.yml docker/nginx-production.conf root@your-server:/opt/kreo-it/

# 3. На сервере
ssh root@your-server
cd /opt/kreo-it
docker load < kreo-it-production.tar.gz
docker-compose up -d production
```

## 🔄 Workflow разработки

### Обычный процесс:

1. **Разработка локально:**
   ```bash
   npm run dev
   # Делаете изменения, тестируете
   ```

2. **Проверка сборки:**
   ```bash
   npm run build:production
   npm run preview
   # Проверяете, что все работает
   ```

3. **Коммит и пуш:**
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push origin main
   ```

4. **Автоматический деплой:**
   - GitHub Actions автоматически задеплоит на kreo.pro
   - Проверьте результат на https://kreo.pro

### Для больших изменений:

1. Создайте feature branch
2. Разрабатывайте локально
3. Тестируйте сборку
4. Merge в main
5. Автоматический деплой

## 📦 Структура окружений

### Локальное окружение (.env)
```env
VITE_APP_ENV=development
VITE_APP_URL=http://localhost:5173
VITE_API_URL=http://localhost:3000
```

### Продакшен окружение (.env.production)
```env
VITE_APP_ENV=production
VITE_APP_URL=https://kreo.pro
VITE_API_URL=https://api.kreo.pro
```

## 🐛 Отладка

### Локально:
```bash
# Запуск с логами
npm run dev

# Проверка линтера
npm run lint

# Исправление ошибок линтера
npm run lint:fix
```

### На сервере:
```bash
# Логи Docker контейнера
docker logs kreo-it-production

# Логи Nginx
tail -f /var/log/nginx/kreo.pro.error.log

# Статус контейнера
docker ps
docker-compose ps
```

## ✅ Чеклист перед деплоем

- [ ] Код работает локально (`npm run dev`)
- [ ] Сборка проходит без ошибок (`npm run build:production`)
- [ ] Preview версия работает (`npm run preview`)
- [ ] Линтер не выдает ошибок (`npm run lint`)
- [ ] Изменения протестированы
- [ ] Коммит с понятным сообщением

## 🔍 Мониторинг после деплоя

1. Проверить доступность: https://kreo.pro
2. Проверить SSL: должен быть зеленый замочек
3. Проверить консоль браузера на ошибки
4. Проверить логи на сервере (если есть проблемы)

## 📝 Полезные команды

```bash
# Разработка
npm run dev              # Запуск dev сервера
npm run build:production # Сборка для продакшена
npm run preview          # Просмотр собранной версии

# Docker
npm run docker:build     # Сборка образа
npm run docker:up        # Запуск контейнера
npm run docker:down      # Остановка контейнера
npm run docker:logs      # Просмотр логов

# Качество кода
npm run lint            # Проверка кода
npm run lint:fix        # Автоисправление ошибок
```

## 🎯 Преимущества упрощенной архитектуры

1. **Простота** - меньше конфигураций, меньше точек отказа
2. **Скорость** - быстрее настройка и деплой
3. **Экономия** - один сервер вместо двух
4. **Достаточно для старта** - можно добавить тестовый сервер позже

## 🔮 Когда добавить тестовый сервер

Добавьте тестовый сервер, если:
- Нужно тестировать перед продакшеном
- Работает команда разработчиков
- Нужны staging тесты
- Критически важны изменения

Пока можно обходиться локальной разработкой и продакшеном.

