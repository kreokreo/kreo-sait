# ✅ Сервер настроен! Следующие шаги

## 🎉 Статус сервера

**IP адрес**: `31.130.155.38`  
**ОС**: Ubuntu 24.04.3 LTS  
**Статус**: ✅ Полностью настроен

### ✅ Установлено:
- ✅ Docker 29.1.1
- ✅ Docker Compose 2.40.3
- ✅ Nginx 1.24.0
- ✅ Certbot 2.9.0
- ✅ Структура директорий создана
- ✅ Systemd service настроен
- ✅ SSH ключ добавлен

## 📋 Следующие шаги

### 1. Настройка DNS (СДЕЛАЙТЕ СЕЙЧАС)

В панели управления доменом `kreo.pro` добавьте:

```
Тип: A
Имя: @ (или оставить пустым)
Значение: 31.130.155.38
TTL: 3600
```

Для www (опционально):
```
Тип: CNAME
Имя: www
Значение: kreo.pro
TTL: 3600
```

**Важно**: DNS изменения могут распространяться до 24-48 часов.

**Проверка DNS:**
```bash
nslookup kreo.pro
# Должен вернуть: 31.130.155.38
```

### 2. Получение SSL сертификата

**После того, как DNS записи распространились** (проверьте через `nslookup`):

```bash
# Подключитесь к серверу
ssh -i ~/.ssh/kreo-timeweb root@31.130.155.38

# Получите SSL сертификат
certbot --nginx -d kreo.pro -d www.kreo.pro
```

Certbot автоматически:
- Получит SSL сертификат от Let's Encrypt
- Настроит Nginx для HTTPS
- Настроит автообновление сертификата

### 3. Настройка Nginx

```bash
# На сервере
cd /opt/kreo-it

# Скопируйте конфигурацию Nginx (после деплоя файлов)
cp docker/nginx-production.conf /etc/nginx/sites-available/kreo.pro
ln -s /etc/nginx/sites-available/kreo.pro /etc/nginx/sites-enabled/

# Проверьте конфигурацию
nginx -t

# Перезапустите Nginx
systemctl reload nginx
```

### 4. Настройка GitHub Secrets

В GitHub: **Settings → Secrets and variables → Actions → New repository secret**

Добавьте:

| Secret Name | Значение |
|------------|----------|
| `PRODUCTION_SERVER_HOST` | `31.130.155.38` |
| `PRODUCTION_SERVER_USER` | `root` |
| `PRODUCTION_SERVER_SSH_KEY` | Содержимое `~/.ssh/kreo-timeweb` |
| `PRODUCTION_SERVER_PORT` | `22` |

**Получить приватный ключ:**
```bash
cat ~/.ssh/kreo-timeweb
# Скопируйте ВЕСЬ вывод (включая -----BEGIN и -----END)
```

### 5. Первый деплой

#### Вариант A: Автоматический (через GitHub Actions)

```bash
# На вашем компьютере
git add .
git commit -m "Настройка деплоя на Timeweb"
git push origin main
```

GitHub Actions автоматически задеплоит проект!

#### Вариант B: Ручной деплой

```bash
# На вашем компьютере
cd /Users/antontusnakov/Desktop/kreo-it
./scripts/deploy.sh

# Скопируйте файлы на сервер
scp -i ~/.ssh/kreo-timeweb \
  kreo-it-production.tar.gz \
  docker-compose.yml \
  docker/nginx-production.conf \
  root@31.130.155.38:/opt/kreo-it/

# На сервере
ssh -i ~/.ssh/kreo-timeweb root@31.130.155.38
cd /opt/kreo-it
docker load < kreo-it-production.tar.gz
docker-compose up -d production
```

### 6. Проверка работоспособности

```bash
# На сервере
docker ps
docker logs kreo-it-production
curl http://localhost/health

# В браузере
# Откройте https://kreo.pro
# Проверьте SSL сертификат (зеленый замочек)
# Проверьте консоль браузера на ошибки
```

## 🔍 Полезные команды

### Подключение к серверу:
```bash
ssh -i ~/.ssh/kreo-timeweb root@31.130.155.38
```

### Проверка сервисов:
```bash
# Docker
docker ps
docker-compose logs -f production

# Nginx
systemctl status nginx
nginx -t

# Логи
tail -f /var/log/nginx/kreo.pro.access.log
tail -f /var/log/nginx/kreo.pro.error.log
```

### Мониторинг:
```bash
docker stats
df -h
free -h
```

## ✅ Чеклист готовности

- [x] Сервер создан в Timeweb
- [x] SSH ключ добавлен на сервер
- [x] Сервер настроен (Docker, Nginx, Certbot)
- [ ] DNS записи настроены
- [ ] DNS записи распространились (проверка через nslookup)
- [ ] SSL сертификат получен
- [ ] Nginx конфигурация скопирована
- [ ] GitHub Secrets настроены
- [ ] Проект задеплоен
- [ ] Сайт доступен по https://kreo.pro

## 🎯 Приоритет действий

1. **СЕЙЧАС**: Настройте DNS записи для kreo.pro
2. **Через 1-2 часа**: Проверьте распространение DNS (`nslookup kreo.pro`)
3. **После DNS**: Получите SSL сертификат
4. **После SSL**: Настройте GitHub Secrets
5. **После Secrets**: Задеплойте проект

---

**Сервер готов!** Осталось только настроить DNS и задеплоить проект. 🚀

