# Архитектура кейсов - Документация

## 📋 Обзор

Этот документ описывает улучшенную структуру кода для работы с кейсами на главной странице и странице кейсов.

## 🎯 Проблемы, которые были решены

1. **Дублирование кода** - Разметка карточек кейсов повторялась в 3 местах
2. **Хардкод проектов** - Наши проекты были захардкожены на главной странице
3. **Сложная логика видео** - 160+ строк логики видео-анимации были встроены в компонент страницы
4. **Нет переиспользуемых компонентов** - Каждая страница имела свою реализацию
5. **Нет единой точки управления данными** - Фильтрация и поиск дублировались

## 🏗️ Новая структура

### 1. Компоненты (`src/components/cases/`)

#### `CaseCard.jsx`
Переиспользуемый компонент для отображения карточки кейса с тремя вариантами:
- **`grid`** - для главной страницы (с видео-анимацией)
- **`list`** - для страницы всех кейсов (горизонтальная карточка)
- **`related`** - для похожих кейсов (компактная версия)

**Использование:**
```jsx
<CaseCard
    caseItem={caseItem}
    index={0}
    showVideo={true}
    isHovered={false}
    onMouseEnter={() => {}}
    onMouseLeave={() => {}}
    videoRefCallback={(el) => {}}
    variant="grid"
/>
```

#### `CaseGrid.jsx`
Компонент сетки кейсов с автоматической поддержкой видео-анимации при наведении.

**Использование:**
```jsx
<CaseGrid 
    cases={cases} 
    showVideo={true} 
    className="custom-class"
/>
```

### 2. Хуки (`src/hooks/`)

#### `useCaseVideoAnimation.js`
Хук для управления видео-анимацией кейсов при наведении. Инкапсулирует всю логику:
- Проигрывание видео вперед при наведении
- Плавная обратная перемотка при уходе мыши
- Управление состоянием видео элементов

**Использование:**
```jsx
const { videoRefs } = useCaseVideoAnimation(totalCases, hoveredCase);
```

### 3. Утилиты (`src/lib/cases.js`)

Централизованные функции для работы с кейсами:

- **`getFilteredCases(filters)`** - Фильтрация кейсов по поиску, тегам, направлению
- **`getRelatedCases(caseId, limit)`** - Получение похожих кейсов по тегам
- **`getCaseBySlug(slug)`** - Получение кейса по slug
- **`getHomePageCases(casesLimit, includeOurProjects)`** - Получение кейсов для главной страницы

**Пример использования:**
```jsx
// Фильтрация
const filtered = getFilteredCases({
    searchQuery: 'стоматология',
    tags: ['Сайт', 'SEO'],
    direction: 'razrabotka',
    limit: 10
});

// Похожие кейсы
const related = getRelatedCases('dental-deal-clinic', 3);

// Главная страница
const homeCases = getHomePageCases(6, true);
```

### 4. Константы (`src/constants/cases.js`)

Все кейсы и наши проекты теперь в одном месте:
- Массив `allCases` содержит все кейсы
- Наши проекты помечены флагом `isOurProject: true`
- Вспомогательные функции: `getAllTags()`, `getAllDirections()`

## 📝 Обновленные страницы

### Главная страница (`src/app/page.jsx`)

**Было:**
- 160+ строк логики видео-анимации
- Хардкод наших проектов
- Дублирование разметки карточек

**Стало:**
```jsx
import { getHomePageCases } from '@/lib/cases';
import { CaseGrid } from '@/components/cases';

const cases = getHomePageCases(6, true);

// В JSX:
<CaseGrid cases={cases} showVideo={true} />
```

### Страница кейсов (`src/app/kejsy/page.jsx`)

**Было:**
- Дублирование логики фильтрации
- Собственная разметка карточек

**Стало:**
```jsx
import { getFilteredCases } from '@/lib/cases';
import { CaseCard } from '@/components/cases';

const filteredCases = getFilteredCases({
    searchQuery,
    tags: selectedTags
});

// В JSX:
{filteredCases.map((caseItem, i) => (
    <CaseCard
        key={caseItem.id}
        caseItem={caseItem}
        index={i}
        variant="list"
    />
))}
```

### Страница отдельного кейса (`src/app/kejsy/[slug]/page.jsx`)

**Было:**
- Поиск кейса через `allCases.find()`
- Ручная фильтрация похожих кейсов

**Стало:**
```jsx
import { getCaseBySlug, getRelatedCases } from '@/lib/cases';
import { CaseCard } from '@/components/cases';

const caseItem = getCaseBySlug(slug);
const relatedCases = getRelatedCases(caseItem.id, 3);

// В JSX:
{relatedCases.map((relatedCase, i) => (
    <CaseCard
        key={relatedCase.id}
        caseItem={relatedCase}
        index={i}
        variant="related"
    />
))}
```

## ✅ Преимущества новой структуры

1. **DRY (Don't Repeat Yourself)** - Код не дублируется
2. **Переиспользуемость** - Компоненты можно использовать везде
3. **Поддерживаемость** - Изменения в одном месте применяются везде
4. **Читаемость** - Код стал проще и понятнее
5. **Тестируемость** - Компоненты и утилиты легко тестировать
6. **Масштабируемость** - Легко добавлять новые варианты отображения

## 🚀 Как добавить новый вариант отображения

1. Добавьте новый вариант в `CaseCard.jsx`:
```jsx
if (variant === 'new-variant') {
    return (
        // Ваша разметка
    );
}
```

2. Используйте его:
```jsx
<CaseCard caseItem={item} variant="new-variant" />
```

## 📦 Структура файлов

```
src/
├── components/
│   └── cases/
│       ├── CaseCard.jsx      # Компонент карточки кейса
│       ├── CaseGrid.jsx      # Компонент сетки кейсов
│       └── index.js          # Экспорты
├── hooks/
│   └── useCaseVideoAnimation.js  # Хук для видео-анимации
├── lib/
│   └── cases.js              # Утилиты для работы с кейсами
└── constants/
    └── cases.js              # Данные кейсов
```

## 🔄 Миграция

Все изменения обратно совместимы. Старый код продолжит работать, но рекомендуется постепенно переходить на новую структуру.

