# Быстрый старт: Timeweb сервер

## 🎯 Краткая инструкция

### 1. Создание сервера в Timeweb

✅ **Выбранные параметры:**
- Ubuntu 24.04
- Москва
- 1 CPU, 2 GB RAM, 30 GB
- Публичный IP
- Бэкапы включены

**Действия:**
1. Нажмите "Заказать"
2. Сохраните IP адрес сервера
3. Загрузите SSH ключ (см. ниже)

### 2. Создание SSH ключа

```bash
# Выполните на вашем компьютере
ssh-keygen -t rsa -b 4096 -f ~/.ssh/kreo-timeweb

# Покажите публичный ключ
cat ~/.ssh/kreo-timeweb.pub
```

**Скопируйте вывод** и загрузите в Timeweb в разделе "Авторизация".

### 3. Подключение к серверу

```bash
# После создания сервера
ssh -i ~/.ssh/kreo-timeweb root@YOUR_SERVER_IP
```

### 4. Настройка сервера

```bash
# На сервере выполните:
curl -fsSL https://raw.githubusercontent.com/your-username/kreo-it/main/scripts/setup-server.sh | bash
```

Или вручную скопируйте и выполните скрипт `scripts/setup-server.sh`.

### 5. Настройка DNS

В панели управления доменом добавьте:
```
A запись: @ → [IP сервера]
CNAME: www → kreo.pro
```

### 6. SSL сертификат

```bash
# После распространения DNS (24-48 часов)
certbot --nginx -d kreo.pro -d www.kreo.pro
```

### 7. GitHub Secrets

В GitHub: Settings → Secrets → Actions

Добавьте:
- `PRODUCTION_SERVER_HOST` = IP сервера
- `PRODUCTION_SERVER_USER` = `root`
- `PRODUCTION_SERVER_SSH_KEY` = содержимое `~/.ssh/kreo-timeweb`
- `PRODUCTION_SERVER_PORT` = `22`

### 8. Первый деплой

```bash
git push origin main
```

GitHub Actions автоматически задеплоит проект!

---

**Подробная инструкция**: см. `TIMEWEB_SETUP.md`

